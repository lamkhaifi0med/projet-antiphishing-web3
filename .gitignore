# --- Secrets (NEVER commit) ---
.env
.env.*
!.env.example
*.key
*.pem
secrets/
# Local credential notes; never store or commit private keys here.
comptes/

# --- Node ---
node_modules/
npm-debug.log*

# --- Hardhat ---
contracts/cache/
contracts/artifacts/
contracts/typechain-types/
contracts/coverage/
contracts/coverage.json
contracts/slither-report.json

# --- n8n local data ---
n8n/data/

# --- Cache capture-pages (contenu extrait de vraies pages, y compris de
# phishing) : reste hors Git par decision explicite (option C). Voir
# ai/dataset/final/cache-index.json (verse) pour la traçabilite/verification.
ai/dataset/final/.cache/

# --- Python (ai eval scripts if any) ---
__pycache__/
*.pyc
.venv/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.vscode/settings.json

# Documents de handoff locaux (contiennent des references aux secrets)
docs/HANDOFF_BINOME.md
SECRETS_WHATSAPP.txt
